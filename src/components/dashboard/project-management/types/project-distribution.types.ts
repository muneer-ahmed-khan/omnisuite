/**
 * Defines the structure for a dropdown menu item in the Project Distribution component.
 */
export interface DropdownItem {
  icon: string
  text: string
}

/**
 * Represents the distribution data for the project distribution chart.
 * This is simply an array of numbers.
 */
export type DistributionData = number[]
