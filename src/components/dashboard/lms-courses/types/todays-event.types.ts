/**
 * TodaysEvent Types
 * -----------------------------------------------------------------------------
 * Type definitions for event items and component props
 */

export interface Event {
  id: number
  image: string
  title: string
  speakers: string
  topics: string
  start: string
  progress: string
}
