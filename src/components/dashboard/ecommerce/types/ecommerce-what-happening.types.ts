/**
 * WelcomeStat Interface
 *
 * Represents a single stat item shown in the WhatHappening card.
 */
export interface WelcomeStat {
  label: string // Label for the stat (e.g., TODAY’S VISIT)
  value: string // Value of the stat (e.g., 15,209)
}
