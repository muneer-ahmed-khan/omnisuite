/**
 * EarningsBreakdownItem Interface
 *
 * Represents a single item in the earnings category breakdown list.
 */
export interface EarningsBreakdownItem {
  category: string // The name of the category (e.g. Groceries)
  amount: string // Revenue amount for the category (e.g. "$9,500")
}
