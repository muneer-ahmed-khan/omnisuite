/**
 * Deal represents a single deal entry in the DealsList component.
 */
export interface Deal {
  id: string
  name: string
  amount: string
  status: string
  probability: string
  closingDate: string
}
